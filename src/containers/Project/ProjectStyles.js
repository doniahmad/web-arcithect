import styled from "styled-components";

const Styles = styled.div`
  .title {
    font-weight: 300;
    font-size: 64px;
    line-height: 64px;
    color: #bdbdbd;
  }

  .title span {
    font-weight: bold;
    font-size: 64px;
    line-height: 64px;
    color: #333333;
  }

  .main {
    overflow: hidden;
    margin: 2rem auto;
  }
`;

export default Styles;
